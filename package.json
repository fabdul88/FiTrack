{
  "name": "fitrack-server",
  "version": "1.0.0",
  "description": "Fitrack app",
  "main": "server.js",
  "scripts": {
    "test": "jest",
    "build": "cd fitrack-client && npm run build",
    "install-client": "cd fitrack-client && npm install",
    "heroku-postbuild": "NPM_CONFIG_PRODUCTION=FALSE npm run install --prefix client && npm run build --prefix client",
    "start": "node server.js",
    "client": "cd fitrack-client && npm start"
  },
  "author": "Abdul Farhan",
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "mongoose": "^5.10.9",
    "nodemon": "^2.0.5"
  }
}
