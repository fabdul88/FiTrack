{
  "name": "fitrack-server",
  "version": "1.0.0",
  "description": "Fitrack app",
  "main": "server.js",
  "scripts": {
    "test": "jest",
    "build": "cd fitrack-client && npm run build",
    "install-client": "npm install --prefix fitrack-client",
    "start": "node server.js",
    "client": "npm start --prefix fitrack-client",
    "heroku-postbuild": "NPM_CONFIG_PRODUCTION=FALSE npm run install --prefix fitrack-client && npm run build --prefix fitrack-client"
  },
  "author": "Abdul Farhan",
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^8.2.0",
    "express": "^4.17.1",
    "mongoose": "^5.10.9",
    "nodemon": "^2.0.5"
  }
}
